# @robotico/result

Result type for TypeScript. Explicit success/error: `Ok(value)` or `Err(error)`, with map, mapErr, match, bind. Aligned with Robotico C# and ArkTS.

## Installation

```bash
npm install @robotico/result
```

## Usage

*(Implement Ok/Err, map, mapErr, match, bind; optional async variants.)*

## License

MIT
